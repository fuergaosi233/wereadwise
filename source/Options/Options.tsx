import * as React from 'react';

import './styles.scss';

const Options: React.FC = () => {
  return (
    <div>
      {/* Notion to desplay only egg */}
      <h1>Nothing</h1>
    </div>
  );
};

export default Options;
